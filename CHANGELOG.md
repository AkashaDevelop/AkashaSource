# Changelog

本文件记录 AKasha 每个版本的更新内容。发布工作流会自动提取对应版本的日志作为 GitHub Release 说明。

格式规范：
- 每个版本以 `## [版本号] - YYYY-MM-DD` 开头
- 版本号应与 git tag 一致（如 `v1.0.0Beta`）
- 变更类型分三类：`### 新增` / `### 优化` / `### 修复`

---

## [v1.1.0Beta] - 2026-07-18

### 新增
- 分组系统重构：分组可配置「公开 / 隐藏」可见性（隐藏分组仅特殊授权或直接分配的用户可用），计费倍率收归分组表统一管理
- 特殊分组授权：配置「用户到某基础分组后自动解锁的额外分组」，支持解锁隐藏分组（如 svip → ex），启动时自动从旧配置迁移
- 用户可直接授予多个额外分组，与基础分组共存，贯穿准入校验与计费全链路
- 特殊分组倍率：在分组编辑内配置「本分组用户使用某分组时的专属倍率」，覆盖目标分组默认倍率
- 分组管理改为卡片式布局，编辑弹窗集中管理可见性 / 解锁分组 / 限速 / 倍率 / 模型倍率覆盖 / 特殊分组倍率 / 允许渠道
- 获取模型对话框：渠道添加 / 编辑时从上游拉取模型后弹窗挑选，支持搜索、新旧模型分区、按供应商分组勾选后再写回
- 新增渠道并设置模型后自动同步到模型元数据（模型广场），新增模型元数据时自动补建定价占位
- 模型广场详情新增「按分组定价」：按当前有权限使用的分组展示价格（游客仅 default，登录用户按其可用分组），含分组倍率 / 输入 / 输出 / 缓存
- 模型定价页新增「仅显示广场未匹配定价」筛选与待配价标记，提示超管为已收录但未配价的模型手动设置
- 模型广场全面重构对齐 new-api 新版前端：侧栏多维筛选（价格档位 / 分组 / 供应商 / 端点 / 能力标签）、卡片与表格双视图、/1M 与 /1K 计价单位切换、全局搜索增强
- 模型详情抽屉三标签页：概览（定价 / 规格 / 计费示例）、性能（基于真实调用日志的 TPS / 平均延迟 / 成功率 / 近 7 天请求趋势）、API（接入信息 / 身份验证 / 多语言调用示例 / 支持的参数表 / 速率限制表）
- 官方元数据仓库同步：一键同步自动带出模型描述、图标、标签、端点、供应商与官方价格，支持中 / 英 / 日多语言元数据，供应商自动建档
- 同步向导对话框：同步源与语言选择，对齐 new-api 交互
- 分组三维速率限制：RPM（每分钟请求）/ TPM（每分钟 Token）/ RPD（每日请求），Redis 优先内存兜底，五个中继入口统一生效
- 分组表为空时自动播种 default / vip / svip 三个默认分组，可自由修改
- 模型元数据批量操作：批量启用 / 禁用 / 删除，带全选复选框
- 模型元数据侧边抽屉编辑器：匹配规则选择、端点可视化编辑与模板填充、标签与定价配置
- 渠道对话框「可用分组」改为多选下拉，选项来自分组管理
- 公开模型统计接口 `/api/pricing/stats`：真实日志聚合的模型性能数据

### 优化
- 多选下拉统一显示所选项名称，替代「已选 N 项」的模糊计数
- 模型中心合并供应商管理，移除独立的「供应商与部署」菜单，旧路由自动重定向
- 移除部署管理页面与 io.net 部署能力（控制器 / 模型 / SDK / 路由全量清理）
- 所有渠道 ID 手输框改为下拉选择：模型元数据渠道过滤（多选）、玄鉴审核渠道、日志渠道筛选
- 模型配置 / 订阅套餐 / 分组管理页面增加分页（每页 20 条）
- 宸汐清源规则缓存刷新间隔从 30 秒调整为 5 分钟，仅在规则数量变化时输出日志
- 移除与元数据重合的冗余模型配置页面

### 修复
- 修复渠道添加时「从上游拉取」快捷按钮点击无反应的问题
- 修复「从上游同步参考价」会为上游全部模型新建定价记录、污染模型广场配置的问题（改为仅更新广场已收录模型的参考价）
- 修复模型元数据编辑抽屉中匹配规则无法选中的问题
- 修复批量操作请求被 `/models/:id` 路由拦截导致「模型 ID 非法」的问题
- 修复官方元数据同步中文路径 404（上游多语言目录为 zh 而非 zh-CN）
- 修复端点配置 `null` 值被写成字符串入库的问题

---

## [v1.0.0Beta] - 2026-07-08

### 新增
- 多验证码方式支持（Turnstile / GeeTest / hCaptcha / reCAPTCHA）
- Passkey / WebAuthn 无密码登录
- 双因素认证（2FA/TOTP）
- 宸汐玄鉴 AI 内容审核模块，支持行为风控与上下文净化
- 渠道管理与供应商部署中心，支持渠道商角色体系
- 单文件部署模式：前端产物嵌入后端二进制，开箱即跑

### 优化
- 重构令牌分组架构，统一对齐权限与分组逻辑
- 重构额度计费与展示全流程，计费口径更加清晰准确
- 初始化向导增加数据库类型选择，支持 SQLite/MySQL/PostgreSQL 三种驱动配置

### 修复
- 修复 garble 交叉编译在非 linux-amd64 平台的 exec format error
- 修复前端 API 客户端路径导入错误及表单组件属性缺失
- 修复宸汐御、宸汐清源、宸汐玄鉴三大安全模块的若干问题
