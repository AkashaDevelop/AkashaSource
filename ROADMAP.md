# 项目名称：Akasha (阿卡夏)

**Akasha** 是一个旨在汇聚全宇宙智慧（LLM）的 AI 网关系统。它致力于成为连接用户与各类 AI 服务的坚实桥梁，不仅提供基础的模型转发功能，更深度融合了现代化的用户管理、风控体系以及特色社区（如 LinuxDO）生态。

## 📅 开发路线图 (Roadmap)

本文档记录了项目当前相对于成熟竞品（如 NewAPI/OneAPI）尚未实现或需要优化的功能模块，作为后续版本迭代的指导。

### 🔴 优先级：高 (Critical)
**影响核心稳定性与可用性的功能**

1.  **渠道自动熔断与恢复 (Auto-Ban & Recovery)**
    *   **现状**: ✅ 已实现基于错误代码（如 401, 402, Insufficient Quota）的自动熔断机制。
    *   **目标**:
        *   ✅ 实现基于错误率或连续失败次数的自动熔断机制（自动禁用渠道）。
        *   ✅ 引入“半开”状态或定时任务，自动检测并恢复已禁用的渠道（已实现每小时自动探测恢复）。
        *   支持配置熔断阈值和恢复检测策略。
2.  **请求速率限制 (Rate Limiting)**
    *   **现状**: ✅ 已实现基于内存的滑动窗口限流。
    *   **目标**:
        *   ✅ 实现基于 Token、用户、IP 的多级限流（Token 限流 > IP 限流）。
        *   ✅ 支持 Redis 分布式限流（为横向扩展做准备）。
3.  **数据持久化与迁移增强**
    *   **现状**: ✅ 已实现迁移版本表与 SQL 迁移文件管理，支持回滚。
    *   **目标**: 持续补充 SQL 迁移文件与回滚覆盖率。
4.  **渠道高级配置补全**
    *   **现状**: ✅ 渠道模型已添加 `Proxy` 字段，支持为特定渠道配置 HTTP 代理。
    *   **目标**: 
        *   ✅ 增加 `Proxy` 字段支持，允许为特定渠道设置 SOCKS5/HTTP 代理（已支持）。
        *   ✅ 增加自定义 `Headers` 字段，支持特殊认证需求。

### 🟡 优先级：中 (Important)
**提升运营能力与用户体验的功能**

1.  **API 能力补全 (RAG & Audio)**
    *   **现状**: ✅ 已实现 `/v1/embeddings`、Audio 与 `/v1/files` 接口。
    *   **目标**:
        *   ✅ 实现 `/v1/embeddings` 接口（RAG 应用核心）。
        *   ✅ 实现 `/v1/audio/transcriptions` (Whisper) 和 `/v1/audio/speech` (TTS)。
        *   ✅ 实现 `/v1/files` 接口。
2.  **邀请返利机制 (Affiliate System)**
    *   **现状**: ✅ 基础功能已实现。
    *   **目标**:
        *   ✅ 生成用户专属邀请链接/邀请码。
        *   ✅ 实现邀请注册双方自动获赠额度。
        *   ✅ 提供邀请记录查询 API。
3.  **更多模型协议适配**
    *   **现状**: ✅ 支持 OpenAI, Claude, Gemini, Midjourney 与国产模型原生协议。
    *   **目标**: ✅ 增加对 Azure OpenAI, 阿里通义千问, 腾讯混元, 百度文心一言, 讯飞星火等主流国产大模型的原生支持。
4.  **内容安全审查 (Content Moderation)**
    *   **现状**: ✅ 已实现关键词拦截、第三方审核接口对接与白名单策略。
    *   **目标**:
        *   ✅ 集成敏感词库过滤。
        *   ✅ 支持对接第三方内容审查 API，拦截违规 Prompt 或 Completion。
5.  **数据与报表增强**
    *   **现状**: ✅ 已实现日活用户(DAU)、错误率趋势图。
    *   **目标**:
        *   ✅ 支持按天查看请求量、成功率、活跃用户数。
        *   ✅ 支持将消费日志、充值记录导出为 CSV，并支持筛选条件。

### 🟢 优先级：低 (Nice to Have)
**锦上添花的功能**

1.  **系统性能优化 (Redis)**
    *   **现状**: ✅ 已实现 Redis 缓存 Token 与日志异步批量写入。
    *   **目标**: 引入 Redis 缓存 Token 信息，减少高并发下的数据库压力；实现日志异步批量写入。
2.  **第三方支付对接**
    *   **目标**: ✅ 已完成易支付对接（订单创建与回调入账）。
4.  **令牌管理增强**
    *   **目标**: 支持 Token 绑定 IP、设置过期时间、限制可用模型范围。

## 📊 功能对比矩阵 (vs NewAPI)

| 功能模块 | AIGateway-X (本项目) | NewAPI (OneAPI) | 备注 |
| :--- | :---: | :---: | :--- |
| **基础转发** | ✅ | ✅ | |
| **多渠道负载均衡** | ✅ | ✅ | |
| **渠道自动禁用** | ✅ | ✅ | **已实现半开检测与自动恢复** |
| **Embeddings 接口** | ✅ | ✅ | **已实现** |
| **Audio 接口 (TTS/STT)** | ✅ | ✅ | **已实现** |
| **文件接口** | ✅ | ✅ | **已实现** |
| **用户分组/等级** | ✅ | ✅ | 本项目有 LinuxDO 特色等级同步 |
| **Midjourney 支持** | ✅ (原生集成) | ⚠️ (部分/插件) | 本项目含计费与自动退款闭环 |
| **邀请机制** | ✅ | ✅ | **已实现** |
| **速率限制** | ✅ | ✅ | **已实现多级限流** |
| **HTTP 代理** | ✅ | ✅ | **已支持 SOCKS5/HTTP** |
| **风控验证** | ✅ (Cloudflare Turnstile) | ⚠️ (旧版/简单) | |
| **内容安全审查** | ✅ (关键词拦截) | ✅ | **已实现基础过滤** |
| **国产模型适配** | ✅ (OpenAI兼容) | ✅ | **已支持通用兼容模式** |
| **数据看板** | ✅ (DAU/Error) | ✅ | **已实现增强报表** |

## 🔍 与 NewAPI 仍有差距的功能

1.  **支付渠道多样化**
    *   已实现易支付，但尚缺少 Stripe/支付宝/微信等多渠道适配。
2.  **系统配置可视化**
    *   已提供配置元数据接口，但仍缺少完整前端面板。
3.  **更完整的统计报表**
    *   已支持多维筛选与 CSV 导出，但缺少更丰富的图表与 Excel 导出。

---
> 📝 **开发者备注**: 本项目代码结构清晰，基于 Go + Gin + GORM 构建，易于扩展。建议优先完成 **支付系统**、**内容审查** 与 **Redis 缓存**，以保障服务在生产环境的稳定性和可用性。
