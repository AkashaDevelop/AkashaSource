# 项目名称：Akasha (阿卡夏)

**Akasha** 是一个旨在汇聚全宇宙智慧（LLM）的 AI 网关系统。它致力于成为连接用户与各类 AI 服务的坚实桥梁，不仅提供基础的模型转发功能，更深度融合了现代化的用户管理、风控体系以及特色社区（如 LinuxDO）生态。

## 📅 开发路线图 (Roadmap)

本文档记录了项目当前相对于成熟竞品（如 NewAPI/OneAPI）尚未实现或需要优化的功能模块，作为后续版本迭代的指导。

### 🔴 优先级：高 (Critical)
**影响核心稳定性与可用性的功能**

1.  **渠道自动熔断与恢复 (Auto-Ban & Recovery)**
    *   **现状**: 目前仅在单次请求中进行简单的轮询重试。
    *   **目标**:
        *   实现基于错误率或连续失败次数的自动熔断机制（自动禁用渠道）。
        *   引入“半开”状态或定时任务，自动检测并恢复已禁用的渠道。
        *   支持配置熔断阈值和恢复检测策略。
2.  **请求速率限制 (Rate Limiting)**
    *   **现状**: 无任何限流措施，易受攻击或被单一用户耗尽资源。
    *   **目标**:
        *   实现基于 Token、用户、IP 的多级限流（RPM/RPD）。
        *   支持 Redis 分布式限流（为横向扩展做准备）。
3.  **数据持久化与迁移增强**
    *   **现状**: 依赖 GORM 自动迁移，缺乏版本控制。
    *   **目标**: 引入标准的 SQL 迁移文件管理，确保数据库变更的可追溯与回滚。
4.  **渠道高级配置补全**
    *   **现状**: 渠道模型缺少 `Proxy` 字段，无法配置 HTTP 代理。
    *   **目标**: 
        *   增加 `Proxy` 字段支持，允许为特定渠道设置 SOCKS5/HTTP 代理。
        *   增加自定义 `Headers` 字段，支持特殊认证需求。

### 🟡 优先级：中 (Important)
**提升运营能力与用户体验的功能**

1.  **API 能力补全 (RAG & Audio)**
    *   **现状**: 仅实现了 `/v1/chat/completions` 和 MJ 接口。
    *   **目标**:
        *   实现 `/v1/embeddings` 接口（RAG 应用核心）。
        *   实现 `/v1/audio/transcriptions` (Whisper) 和 `/v1/audio/speech` (TTS)。
        *   实现 `/v1/files` 接口。
2.  **邀请返利机制 (Affiliate System)**
    *   **现状**: 数据库有字段但无业务逻辑。
    *   **目标**:
        *   生成用户专属邀请链接/邀请码。
        *   实现邀请注册双方自动获赠额度。
        *   提供邀请记录查询与统计界面。
3.  **更多模型协议适配**
    *   **现状**: 支持 OpenAI, Claude, Gemini, Midjourney。
    *   **目标**: 增加对 Azure OpenAI, 阿里通义千问, 腾讯混元, 百度文心一言, 讯飞星火等主流国产大模型的原生支持。
4.  **内容安全审查 (Content Moderation)**
    *   **现状**: 无审查。
    *   **目标**:
        *   集成敏感词库过滤。
        *   支持对接第三方内容审查 API，拦截违规 Prompt 或 Completion。

### 🟢 优先级：低 (Nice to Have)
**锦上添花的功能**

1.  **系统性能优化 (Redis)**
    *   **现状**: Token 校验与日志写入均为同步数据库操作。
    *   **目标**: 引入 Redis 缓存 Token 信息，减少高并发下的数据库压力；实现日志异步批量写入。
2.  **数据导出与报表**
    *   **目标**: 支持将消费日志、充值记录导出为 CSV/Excel，提供更直观的图表分析。
3.  **第三方支付对接**
    *   **目标**: 对接易支付、Stripe 等在线支付平台，实现自助充值（目前仅支持兑换码）。
4.  **界面国际化 (i18n)**
    *   **目标**: 支持中英多语言切换。
5.  **令牌管理增强**
    *   **目标**: 支持 Token 绑定 IP、设置过期时间、限制可用模型范围。

## 📊 功能对比矩阵 (vs NewAPI)

| 功能模块 | AIGateway-X (本项目) | NewAPI (OneAPI) | 备注 |
| :--- | :---: | :---: | :--- |
| **基础转发** | ✅ | ✅ | |
| **多渠道负载均衡** | ✅ | ✅ | |
| **渠道自动禁用** | ❌ | ✅ | **需优先补充** |
| **Embeddings 接口** | ❌ | ✅ | **RAG 刚需** |
| **用户分组/等级** | ✅ | ✅ | 本项目有 LinuxDO 特色等级同步 |
| **Midjourney 支持** | ✅ (原生集成) | ⚠️ (部分/插件) | 本项目含计费与自动退款闭环 |
| **邀请机制** | ❌ | ✅ | |
| **速率限制** | ❌ | ✅ | |
| **HTTP 代理** | ❌ | ✅ | |
| **风控验证** | ✅ (Cloudflare Turnstile) | ⚠️ (旧版/简单) | |
| **国产模型适配** | ❌ | ✅ | |

---
> 📝 **开发者备注**: 本项目代码结构清晰，基于 Go + Gin + GORM 构建，易于扩展。建议优先完成 **渠道自动熔断**、**Embeddings 接口** 与 **速率限制**，以保障服务在生产环境的稳定性和可用性。
