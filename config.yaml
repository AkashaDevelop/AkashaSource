# Akasha 开发环境配置

server:
  port: 8080
  mode: debug

database:
  driver: sqlite
  file: akasha.db

# JWT 密钥不再从此文件读取：启动时优先读取环境变量 AKASHA_JWT_SECRET，
# 否则自动生成并写入 backend/jwt_secret.key（该文件已加入 .gitignore）。
